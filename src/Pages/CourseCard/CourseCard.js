import React from 'react';

const CourseCard = ({course}) => {
    const {_id, title, total_view, image_url, author, details, rating} = course;
    console.log(course);
    return (
        <div>
            
        </div>
    );
};

export default CourseCard;